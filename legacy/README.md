# Legacy

Archived previous versions. **Not maintained.**

Use the repository **root** application instead (`wails3 build` → `bin/NFA-Tool.exe`).

| Item | Description |
|------|-------------|
| `GUI.py`, `jwt_helper.py`, `resources.py`, `requirements.txt` | Original Python Steam Cache Login |
| `nfa-tool/` | Early Go + Wails **v2** port |
| `binaries/` | Old `.exe` builds (local only; ignored by git if present) |
